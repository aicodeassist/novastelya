# SEO Indexing Control
# Set to false to block all search engine crawlers (noindex, follow + robots disallow)
# Set to true (or leave empty/undefined) to allow search engine indexing (robots.txt allowance + meta index tag)
NEXT_PUBLIC_ALLOW_INDEXING=true
